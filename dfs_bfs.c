#include<stdio.h>
#include<string.h>

void DFS_recurse(int v,int *visited){
	visited[v] = 1;
	for(i=0; i<n; i++){
		if
	}
}

void DFS_main(int **graph,int n){
	int visited[n];
	for(i=0; i<n; i++){
		visited[i] = 0;
	}
	for(i=1; i<=n; ++i){
			if(visited[i] == 0)
				dfs_recurse(i);
	} 
}